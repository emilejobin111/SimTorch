"""Package version metadata for SimTorch.

This module stores the canonical semantic version tuple and the human-readable
`__version__` string exposed by the package.
"""

# Major, Minor, Revision
VERSION = (2,0,0)

__version__ = ".".join(map(str,VERSION))